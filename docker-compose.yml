services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: bluelock-frontend
    volumes:
      - ./.env.prod:/app/.env
    ports:
      - "9050:80"
    restart: always
